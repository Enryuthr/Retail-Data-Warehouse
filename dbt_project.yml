name: retail_data_platform
version: "1.0.0"
config-version: 2

profile: retail_data_platform

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_data_platform:
    silver:
      +schema: silver
      # ponytail: full rebuild keeps late-arrival and tombstone joins deterministic at this scale.
      +materialized: table
    gold:
      +schema: gold
      # ponytail: full rebuild avoids partial-fact edge cases until measured volume requires it.
      +materialized: table
